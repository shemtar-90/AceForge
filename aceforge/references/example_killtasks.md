# Kill Task Examples — Live Server Reference Examples
These are real SQL files from the Shattered Dawn ACEmulator server.
Use these as the authoritative format for all generated SQL.

## 1000000 RiftEchoKT.sql

```sql
DELETE FROM quest WHERE name = 'RiftEchoKT';
INSERT INTO quest (id, name, min_Delta, max_Solves, message)
VALUES ('1000000', 'RiftEchoKT', '0', '100', 'kill counter');
```

## 1000001 ExperimenterKT.sql

```sql
DELETE FROM quest WHERE name = 'ExperimenterKT';
INSERT INTO quest (id, name, min_Delta, max_Solves, message)
VALUES ('1000001', 'ExperimenterKT', '0', '100', 'kill counter');
```

## 1000002 RuschkScrapperKT.sql

```sql
DELETE FROM quest WHERE name = 'RuschkScrapperKT';
INSERT INTO quest (id, name, min_Delta, max_Solves, message)
VALUES ('1000002', 'RuschkScrapperKT', '0', '100', 'kill counter');
```

## 1000003 ElementalRiftEssenceKT.sql

```sql
DELETE FROM quest WHERE name = 'ElementalRiftEssenceKT';
INSERT INTO quest (id, name, min_Delta, max_Solves, message)
VALUES ('1000003', 'ElementalRiftEssenceKT', '0', '100', 'kill counter');
```

## 1000004 FailedExperimentKT.sql

```sql
DELETE FROM quest WHERE id = '1000004' OR name = 'FailedExperimentKT';
INSERT INTO quest (id, name, min_Delta, max_Solves, message)
VALUES ('1000004', 'FailedExperimentKT', '0', '100', 'kill counter');
```

## 1000005 RiftBeastKT.sql

```sql
DELETE FROM quest WHERE id = '1000005' OR name = 'RiftBeastKT';
INSERT INTO quest (id, name, min_Delta, max_Solves, message)
VALUES ('1000005', 'RiftBeastKT', '0', '100', 'kill counter');
```

## 1000000 RiftEchoKT.sql

```sql
DELETE FROM quest WHERE name = 'RiftEchoKT';
INSERT INTO quest (id, name, min_Delta, max_Solves, message)
VALUES ('1000000', 'RiftEchoKT', '0', '100', 'kill counter');
```

## 1000001 ExperimenterKT.sql

```sql
DELETE FROM quest WHERE name = 'ExperimenterKT';
INSERT INTO quest (id, name, min_Delta, max_Solves, message)
VALUES ('1000001', 'ExperimenterKT', '0', '100', 'kill counter');
```

